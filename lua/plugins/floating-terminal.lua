return {
    'numToStr/FTerm.nvim',
    opts = {
        cmd = require("os-config").GetShellName()
    }
    -- config = function()
    --     require("FTerm").setup({
    --         cmd = require("os-config").GetShellName(),
    --     })
    -- end
}
